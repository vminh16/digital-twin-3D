[pytest]
pythonpath = src
testpaths = tests
markers =
    real_data: requires the local ignored dataset and pinned native dependencies
    cuda: requires CUDA-enabled PyTorch and a working gsplat CUDA backend
    lpips_smoke: optional pretrained LPIPS initialization smoke test
